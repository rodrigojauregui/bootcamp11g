
const fs = require('fs')
/*
fs.writeFile('created.txt', 'Hola desde node', 'utf8', (error) => {
    if (error) {
        console.error('no se pudo crear el archivo', error)
        return
    }
    console.log('se escribio el archivo')
})
*/
/*
fs.readFile('created2.txt', 'utf8', (err, data) => {
    if (err){
        console.error('no se pudo leer el archivo', error)
        return
    }
    console.log(data);
  });
*/
/*
fs.appendFile('created2.txt', 'data to append', 'utf8', (err) => {
    if (err){
        console.error('no se pudo modificar el archivo', err)
        return
    }
});
*/
/*
fs.unlink('created.txt', (err) => {
    if (err){
        console.error('no se pudo eliminar el archivo', err)
        return
    }
  });
*/
/*
function callback(err) {
    if (err){
        console.error('no se pudo copiar el archivo', err)
        return
    }
  
fs.copyFile('created.txt', 'created2.txt', callback);
*/